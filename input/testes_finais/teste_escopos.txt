{ Testa variáveis com mesmo nome em escopos diferentes }
programa escopos;

var x: inteiro;

procedimento A;
var x: inteiro;

    { CORREÇÃO: O procedimento B deve ser declarado ANTES do inicio de A }
    procedimento B;
    var x: inteiro;
    inicio
        x := 20;
        escreva(x);   { imprime 20 }
    fim;

inicio
    x := 10;

    B; { Chama o procedimento B declarado acima }

    escreva(x);       { imprime 10 }
fim;

inicio
    x := 1;
    A;
    escreva(x);       { imprime 1 }
fim.

{ Saída esperada:
    20
    10
    1
}
